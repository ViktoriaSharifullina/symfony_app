{% extends 'base.html.twig' %}

{% block title %}Followers
{% endblock %}

{% block bread %}
	>
	<div class="hover:font-semibold mr-2 ml-2">
		<a href="{{ path('app_profile', {id: user.id}) }}">Profile</a>
	</div>

	> Followers
{% endblock %}

{% block body %}
	{{ include ('profile/_profile_header.html.twig') }}
	{{ include ('profile/_profile_tabs.html.twig') }}
	{{ include ('follower/_follower_list.html.twig', {users: user.followers}) }}
{% endblock %}
